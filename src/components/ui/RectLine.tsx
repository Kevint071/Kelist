export default function RectLine() {
  return (
    <div
      className="animate-fadeIn absolute left-1/2 z-10 h-full w-0.5 -translate-x-1/2 transform"
      style={{
        background: "linear-gradient(to bottom, #4A90E2, #A259FF)",
        boxShadow: "0 0 16px rgb(0, 255, 255)",
      }}
    ></div>
  );
}
